# Desarrollo local (proxy Vite → Node / PHP en Laragon)
# VITE_API_URL=
# VITE_PHP_AUTH_URL=

# Producción (Hostinger + Render) — usar al hacer build:
# VITE_API_URL=https://diagramtec-api.onrender.com
# VITE_PHP_AUTH_URL=https://tudominio.com/php

# Proxy local (solo dev, vite.config.js)
VITE_PHP_PATH=/diagram/DiagramTec/php
# VITE_PHP_TARGET=http://localhost
