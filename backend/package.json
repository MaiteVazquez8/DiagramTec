{
  "name": "tecdiagram-backend",
  "version": "0.1.0",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "start:php": "php -S localhost:8000 -t ../php",
    "start:both": "concurrently \"npm:start:php\" \"npm start\"",
    "build": "echo \"Express: sin paso de build\"",
    "test": "echo \"No tests configured\" && exit 0",
    "test:integration": "node scripts/run_integration_tests.js"
  },
  "dependencies": {
    "axios": "^1.16.1",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^6.9.16",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "nodemon": "^3.0.1"
  }
}
